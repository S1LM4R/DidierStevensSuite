language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "nightly"
# command to install dependencies
install:
  - "pip install --upgrade pip"
  - "pip install wheel"
  - "pip wheel --allow-all-external --allow-unverified PIL .  "
  - "pip install --allow-all-external --allow-unverified PIL ."
# command to run tests